package projeto.demo.app;

import com.google.gson.Gson;

public class Conversor {
	
	private Gson gson = new Gson();
	
	public ViaCepResponse converter(String json) {
		ViaCepResponse response = gson.fromJson(json, null)
	}
	}
